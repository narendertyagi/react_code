// import config from './ConfigClient'
// let modules = {}

// config.modules.map(async (item) => {
//   let moduleRequire = require('../modules/'+item.name);
//   let moduleNew = new moduleRequire(config)
//   modules[item.name] = moduleNew
// })

// export default modules